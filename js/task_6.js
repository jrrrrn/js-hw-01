let input;
let total = 0;
const number = 5;
const message = prompt("Enter a number:")

do {
    
    total = (total + number);
    message;
  result = total;
} while (message === null);
console.log(total)